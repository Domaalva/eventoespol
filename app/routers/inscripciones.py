from fastapi import APIRouter

router = APIRouter(prefix="/inscripciones", tags=["Inscripciones - Domenika Arboleda"])

# TODO (Domenika Arboleda): Implementar aquí los endpoints de Inscripciones:
#   - POST /inscripciones             -> Inscribirse a un evento, validando cupo (escritura)
#   - GET  /inscripciones/{usuario}   -> Ver mis inscripciones (lectura)
#
# Reemplaza este archivo completo por tu versión final (routers/inscripciones.py).
