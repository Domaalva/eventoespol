import 'package:flutter/material.dart';

// TODO (Enrique Rosado): Implementar aquí la pantalla de Notificaciones.
// Reemplaza este archivo completo por tu versión final
// (views/notificaciones_screen.dart).
class NotificacionesScreen extends StatelessWidget {
  const NotificacionesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Pantalla de Notificaciones - pendiente (Enrique Rosado)")),
    );
  }
}
