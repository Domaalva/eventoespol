import 'package:flutter/material.dart';

// TODO (Henry Olvera): Implementar aquí la pantalla de Eventos.
// Reemplaza este archivo completo por tu versión final
// (views/eventos_screen.dart).
class EventosScreen extends StatelessWidget {
  const EventosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Pantalla de Eventos - pendiente (Henry Olvera)")),
    );
  }
}
